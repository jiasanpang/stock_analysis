# 桌面端打包

使用 Electron + React 打包为桌面应用。

**开发**：`powershell -ExecutionPolicy Bypass -File scripts\run-desktop.ps1`

**打包 Windows**：`powershell -ExecutionPolicy Bypass -File scripts\build-all.ps1`

**打包 macOS**：`./scripts/build-desktop-macos.sh`

前置：Node.js 18+、Python 3.10+。Windows 需开启开发者模式。
