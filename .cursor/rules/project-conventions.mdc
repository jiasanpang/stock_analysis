---
description: 公众号文章规范与代码规范（结合本仓库）
globs: 公众号/**/*.md, src/**/*.py, api/**/*.py, data_provider/**/*.py
alwaysApply: false
---

# 项目规范：公众号文章与代码

## 一、公众号文章要求（`公众号/` 目录）

### 人称与契机
- 第一人称用「我」，不用「我们」
- 优化/改动类文章可提及「网友反馈」「网友意见」作为契机

### 受众与内容
- 面向不懂代码的读者，避免配置项、环境变量、API 等技术细节
- 重点介绍**优化方向**和**改动说明**，不写 `PICKER_MODE`、`.env`、`BIAS_THRESHOLD` 等
- 用通俗表述：严进模式 / 平衡模式 / 进攻模式，不用 defensive / balanced / offensive

### 结构
- 标题 + 摘要（> 引用块）
- 一、一览或背景（表格概括）
- 二、详细内容（问题 + 优化，分小节）
- 三、小结
- 文末必须有**免责声明**：本项目仅供学习研究，不构成任何投资建议。股市有风险，投资需谨慎。

### 多平台发布合规（公众号 / 小红书）
- 撰稿、润色或改编为小红书等对外稿件时，须同时遵守 `.cursor/rules/social-publishing-compliance.mdc`（微信运营规范与金融类营销红线、小红书社区规范要点、成稿前检查清单）。
- 平台规则会更新，发文前应在微信规则中心与小红书 App「社区规范」核对最新原文。

### 参考
- 现有风格见 `公众号/选股策略优化说明.md`、`公众号/选股模块介绍.md`

---

## 二、代码要求（结合 AGENTS.md）

### 通用
- Python 3.10+，遵循 `src/`、`api/`、`data_provider/` 现有架构
- 配置统一用 `.env`，参见 `.env.example`
- 新增/修改代码注释必须用**英文**

### 风格
- 行宽 120
- `black` + `isort` + `flake8`
- 关键变更需 `py_compile` 或测试验证

### 完成代码后
- **测试**：运行 `./test.sh syntax` 或相关测试，确保变更可运行、无语法/回归问题
- **重构**：视情况做轻量重构（提取函数、统一 import、修正 docstring、更新 CHANGELOG），避免「写完即止」

### 提交前检查
- **无用代码**：检查本次变更中是否有未使用的 import、变量、函数或死代码，若有则**提示用户删除**，不自动删除

### Git
- 未经确认不执行 `git commit`
- commit message 用英文，不添加 `Co-Authored-By`

### 快速检查
```bash
./test.sh syntax
flake8 main.py src/ --max-line-length=120
```

---

## 三、与本仓库的关联

| 模块 | 路径 | 说明 |
|------|------|------|
| 选股服务 | `src/services/stock_picker_service.py` | 量化筛选 + AI 精选，模式/乖离率/亏损股逻辑 |
| 买卖点规则 | `公众号/买卖点规则.md` | 理想买点、止损、目标，与选股规则需一致 |
| 策略汇总 | `公众号/策略规则汇总.md` | 7 条核心理念、乖离率、买点、选股规则 |
| 配置 | `src/config.py` | PICKER_MODE、BIAS_THRESHOLD、PICKER_ALLOW_LOSS 等 |

写公众号文章时，策略描述需与 `公众号/买卖点规则.md`、`公众号/策略规则汇总.md` 保持一致；涉及实现时参考 `stock_picker_service.py` 与 `config.py`。

---

## 四、AI 交互与成本优化 (Cost Optimization)

为了在使用 Cursor 过程中更省钱、更高效，AI 助手需遵循以下原则：
1. **精简输出 (Conciseness)**：不要输出冗长的解释、重复的代码或无关的背景知识。直接给出答案、代码修改或执行结果。
2. **精准修改 (Targeted Edits)**：修改代码时，优先使用局部替换（如 `StrReplace`），绝对避免重写整个文件，除非是新建文件。
3. **减少不必要的读取 (Minimize Reads)**：在确认需要修改前，不要盲目读取大量无关文件。使用 `Grep` 或 `Glob` 精准定位。
4. **并行调用 (Parallel Tool Calls)**：如果需要读取多个文件或执行多个独立命令，务必在一次回复中并行调用工具，减少对话轮数。
5. **避免过度重构 (No Over-engineering)**：除非用户明确要求，否则只做满足当前需求的最小修改，不要主动进行大范围重构。
